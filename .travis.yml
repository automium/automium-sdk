language: node_js

services:
  - docker

node_js:
  - "8"

branches:
  only:
    - master

install:
  - yarn install

script:
  - yarn test

jobs:
  include:
    - stage: release
      node_js: lts/*
      script:
        - npm run semantic-release